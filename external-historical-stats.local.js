/*
Local-only historical stats overlay payload.

Safe default is null to preserve current behavior.
To test overlays locally, copy shape from external-historical-stats.sample.js
and assign a payload object to window.HISTORICAL_STATS_PAYLOAD.
*/
window.HISTORICAL_STATS_PAYLOAD = null;
