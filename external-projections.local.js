/*
Local-only projections overlay payload.

Safe default is null to preserve current behavior.
To test overlays locally, copy shape from external-projections.sample.js
and assign a payload object to window.PROJECTIONS_PAYLOAD.
*/
window.PROJECTIONS_PAYLOAD = null;
